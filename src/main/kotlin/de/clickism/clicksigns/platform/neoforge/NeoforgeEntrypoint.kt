package de.clickism.clicksigns.platform.neoforge
//? if neoforge {
/*import de.clickism.clicksigns.ClickSigns
import net.neoforged.bus.api.IEventBus
import net.neoforged.fml.common.Mod

@Mod(ClickSigns.MOD_ID)
class NeoforgeEntrypoint(eventBus: IEventBus) {
    init {
        ClickSigns.initialize()
        NeoforgePlatform.ITEMS_REGISTRY.register(eventBus)
        NeoforgePlatform.BLOCKS_REGISTRY.register(eventBus)
    }
}
*///?}