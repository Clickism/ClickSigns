package de.clickism.clicksigns.ui;

/**
 * Constants used in the UI module.
 */
public class UiConstants {
    /**
     * Alpha value for inactive elements
     */
    public static final float INACTIVE_ALPHA = .3f;
    /**
     * The scale at which each pixel of the texture is rendered.
     */
    public static final int DEFAULT_TEXTURE_RENDER_SCALE = 4;

    private UiConstants() {
        // Utility class
    }
}
